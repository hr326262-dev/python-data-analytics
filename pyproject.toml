[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "python-data-analytics"
version = "0.1.0"
description = "A complete starter project for Python data analytics"
requires-python = ">=3.10"
